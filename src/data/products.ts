import { Product, Category } from '../types';

// Empty categories array since we're removing categories from frontend
export const categories: Category[] = [];

// Empty products array since we're removing demo products
export const products: Product[] = [];